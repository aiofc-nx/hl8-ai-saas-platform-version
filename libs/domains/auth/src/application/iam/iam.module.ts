import { Module } from "@nestjs/common";

/**
 * @zh
 * @description IAM 应用层模块骨架，用于集中管理身份与权限相关的命令、查询、Saga 与事件处理器。
 * @remarks 当前仅提供占位模块，后续阶段将逐步引入具体实现。
 */
@Module({
  imports: [],
  providers: [],
  exports: [],
})
export class IamModule {}

import { Module } from "@nestjs/common";

/**
 * @zh
 * @description IAM 应用层模块骨架，用于集中管理身份与权限相关的命令、查询、Saga 及事件处理器。
 * @remarks 该模块当前仅作为占位，后续阶段会逐步引入具体实现。
 */
@Module({
  imports: [],
  providers: [],
  exports: [],
})
export class IamModule {}


